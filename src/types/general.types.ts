export type TSidebarLink = {
  title: string;
  icon: JSX.Element;
  path: string;
};
export type TDropdownData = {
  value: string;
  label: string;
};
export type TFilter = {
  text?: string;
  status?: string;
  country?: string;
  type?: string;
  state?: string;
};
